import Foundation
import SwiftData

/// Utilitaire de données - Démarrage propre avec base locale vide
public struct SampleData {
    public static func populate(context: ModelContext) {
        // Aucune donnée factice insérée. L'utilisateur commence avec un jardin vierge.
    }
}
